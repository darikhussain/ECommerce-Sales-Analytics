Part A — Theory
Answer:
SQL stands for Structured Query language. it is a language to communicate with databases.
database are like a library, inside a database their are many tables.
table is like a bookshelf that record information.
row contains information about a specific or one person.
column contains a piece of same information
a query is simply a request you send to the database
because its easy to understand

Part B — Observation
Answer:
there are 4 rows
there are 4 columns
50000
4th column

Part C — AI Practice
Answer:
it use an example of a supermarket where product, category, price and stock are available and it explain every SQL functions like adding a new product, changing information, removing products and so on.


Part A – Theory
Answer:

SELECT display the data or information from database.

* means show all data or show all columns.

FROM means take data from this table.

SELECT Name FROM Students only show Students name column but SELECT * FROM Students shows all the columns

because its shows all columns and if there are many columns then its shows unnecessary data

Part B – Reading SQL

Answer:
Employees table
there are 2 columns will be displayed (Name and Salary)
Name and Salary
No because we only use Name and Salary if we use * then it will shows all columns


Part A – Theory
Answer:
we use WHERE for filter a data to show a specific row
it easy to read and understand which one is a text and number
Exactly equal to.
it not return any data rows.

Part B – Reading SQL
Answer:
Cars
Brand, and Model column displayed.
Black color row displayed.
No because we use WHERE so it will show only color.

Part C – Write SQL
Answer:
SELECT *
FROM cars
WHERE brand = 'Tata';

SELECT Model
FROM cars
WHERE Brand = 'Hyundai';

SELECT Brand, Price
FROM cars
WHERE Brand = 'Mahindra';

SELECT *
FROM cars
WHERE Color = 'White';

 CarID  Brand     Model     Price  Color 
     1  Tata      Nexon    900000  White 
     2  Hyundai   Creta   1200000  Black 
     3  Mahindra  XUV700  1800000  Blue  

1) Tata
2) Black
3) 1800000

Part A – Theory

Answer:

> means above number What we gave them and >= means equal to and above the number.

< means below number What we gave them and <= means equal to and below the number.

!= means not equal to the given number.

when we use > 900000 means above 900000 excluding 900000 and >= 900000 means including 900000 and above.

Part B – Reading SQL
Question 1: Hyundai Creta
            Mahindra XUV700
Question 2: Nexon Venue Creta XUV700
Question 3:  CarID  Brand     Model     Price  Color 
     1  Tata      Nexon    900000  White 
     3  Mahindra  XUV700  1800000  Blue  
     4  Tata      Punch    700000  Red   

Part C – Write SQL
SELECT *
FROM cars
WHERE Price >= 900000;

SELECT Brand, Price
FROM cars
WHERE Price <= 900000;

SELECT Model, Color
FROM cars
WHERE Brand != 'Tata';

SELECT *
FROM cars
WHERE Price < 1000000;

Mini Business Challenge (Think Like a Data Analyst)
1.  SELECT *
    FROM cars
    WHERE Price < 1000000;
    because the owner asked I want to launch a special discount campaign, but only for cars that cost less than ₹10,00,000.
2. Nexon, Punch, and Venue
its qualify
3. To sell
I don't know exactly.

Write SQL queries to:

Show all cars costing more than ₹900000.
Show only the Brand and Price of cars costing ₹900000 or less.
Show the Model and Color of cars that are not Tata.
Show all cars costing less than ₹1000000.


Part A — Theory
Answer:

AND means every conditions are true.

OR means at least one condition must be true.

OR returns more result because OR broadens results.

I want to eat pizza AND burger.
I'm going to bankok OR bali this winter.

Part B — Reading SQL
Question 1: Hyundai Venue 950000 White
Question 2: Tata Nexon
            Tata Punch
            Hyundai Venue
Question 3: XUV700
            Punch

Part C — Write SQL
1)  SELECT *
    FROM Cars
    WHERE Brand = 'Hyundai'
    AND Price > 1000000;

2)  SELECT *
    FROM Cars
    WHERE Brand = 'Tata'
    AND Color = 'White';

3)  SELECT *
    FROM Cars
    WHERE Color = 'Black'
    OR Color = 'White';

4)  SELECT Brand, Model
    FROM Cars
    WHERE Brand = 'Hyundai'
    OR Brand = 'Tata';

Part D – Theory
Answer:
NOT means exclude a condition.

= means same to same NOT = means not same.

Yes.

Part E – Reading SQL
Question 1: Hyundai Creta 1200000 Black
            Mahindra XUV700 1800000 Blue
            Tata Punch 700000 Red
Question 2: Tata Nexon
            Mahindra XUV700
            Tata Punch
Question 3: Nexon
            Creta
            XUV700
            Venue

Part F – Write SQL
1)  SELECT *
    FROM cars
    WHERE NOT Color = 'White';
2)  SELECT Brand, Model
    FROM cars
    WHERE NOT Brand = 'Tata';
3)  SELECT *
    FROM cars
    WHERE NOT Brand = 'Hyundai'
    AND Price > 900000;
4)  SELECT Model, Price
    FROM cars
    WHERE NOT Color = 'Red';

💼 Real Business Challenge:
1) AND
2) because AND simply filter all the delivered orders above 5000.
3)  SELECT *
    FROM Orders
    WHERE Status = 'Delivered'
    AND Amount > 5000;
4) Delivered   7000 
---------------------------------------------
Part A – Theory

Answer:
we use ORDER BY to sort a result.

ASC means lowest to highest and DESC means highest to lowest.

we used LIMIT to sort the result.

Their are used together because they are both acts similar like sort the result.

Part B – Reading SQL
Question 1: Tata Punch 700000
Question 2: Hyundai Venue 
Question 3: Mahindra XUV700 1800000 
            Hyundai  Creta  1200000

Part C – Write SQL

1)  SELECT *
    FROM Cars
    ORDER BY Price DESC;
2)  SELECT *
    FROM Cars
    ORDER BY Brand;
3)  SELECT *
    FROM Cars
    ORDER BY Price
    LIMIT 3;
4)  SELECT Brand, Model, Price
    FROM Cars
    ORDER BY Price DESC
    LIMIT 2;

💼 Real Business Challenge
1) I use ORDER and LIMIT
2) because we need three most expensive products so it can filter or sort the result.
3)  SELECT Product
    FROM Products
    ORDER BY Price DESC
    LIMIT 3;
4)  Laptop
    Phone
    Monitor
---------------------------------------------

Part A – Theory

Answer:

LIKE find pattens in a text.

% means zero or more content.

LIKE used for patten matching and = checks exactly same matching.

we use IN for multiple possible values.

because it gives us a cleaner way to write and easy to read and understand.

Part B – Reading SQL
Question 1: 
Mahindra  XUV700  1800000 
Tata      Nexon    900000 
Tata      Punch    700000 
Question 2: 
 XUV700 
 Nexon
 Punch 
Question 3: 
Alice
Aman
Ankit
Question 4:
Kanpur
Jaipur
Nagpur

Part C – Write SQL
1) SELECT *
FROM Cars
WHERE Brand IN ('Tata','Hyundai');

2) SELECT *
FROM Cars
WHERE Brand NOT IN ('Tata');

3) SELECT Brand, Model
FROM cars
WHERE Brand IN ('Tata','Hyundai','Mahindra');

4) SELECT Customer
FROM Customers
WHERE Customer LIKE 'A%';

💼 Real Business Challenge
1) I use 'IN' operator. 
2) SELECT *
FROM Products
WHERE Category IN ('Clothing','Accessories');
3) T-Shirt
Jacket
Watch
Cap
4) because Clothing and Accessories are high in demand as compare to footwear.

🎤 Interview Question of the Day
We use IN for multiple possible and it is easy to read and understand as compare to OR.
---------------------------------------------

Part A — Theory

Answer:
We use BETWEEN for a value within a range.

Yes.

NULL means unknown or missing value.

NULL means data is missing or unknown but 0 means data in known.

because SQL can't compare a unknown value using normal operators.

Part B — Read the SQL
Question 1:
Tata Nexon 900000
Tata Punch 700000
Hyundai Venue 950000

Question 2:
Nexon
Creta
Venue

Question 3:
Amit
Ravi

Question 4:
John Sarah
Priya David

Part C — Write SQL
1) SELECT *
FROM Cars
WHERE Price BETWEEN 800000 AND 1200000;

2) SELECT Brand, Model, Price
FROM cars
WHERE Price BETWEEN 700000 AND 950000;

3) SELECT Employee
FROM Employees
WHERE Manager IS NULL;

4) SELECT Employee, Department
FROM Employees
WHERE Manager IS NOT NULL;

💼 Real Business Challenge
1) I use IS NULL condition to view which Customer have not provided their phone number.

2) SELECT CustomerName
FROM Customers
WHERE Phone IS NULL;

3)Aman & David will be returned.

4) because if the number is missing then it's difficult to communicate with the Customer while delivering or any other way communicate.

🎤 Interview Question
We us NULL where vale to unknown or not given but 0 means the value is known.
(NULL represents a missing, unknown, or unavailable value. 0 is an actual known numeric value equal to zero. For example, Stock = 0 means the product is out of stock, while Stock = NULL means the stock information is missing or unknown.)
---------------------------------------------
Part A — Theory
Answer:

aggregate functions take many rows of data and return a single summary value.

It count all the the rows including NULl values.

COUNT(*) counts all the rows including NULL value, but COUNT(column_name) does not count NULl values.

SUM() function calculates total sales.

MAX() function finds the highest value.

AVG() function calculates the average.

Part B — Read the SQL
Question 1:
5

Question 2:
101900

Question 3:
20380.00

Question 4:
700

Question 5:
55000

Part C — Write SQL
1) SELECT COUNT(*)
FROM Sales;

2) SELECT SUM(Amount)
FROM Sales;

3) SELECT AVG(Amount)
FROM sales;

4) SELECT MIN(Amount)
FROM Sales;

5) SELECT MAX(Amount)
FROM Sales;

💼 Real Business Challenge
1) There are 5 orders.
2) The total revenue is 30000.
3) The average order Amount is 6000.
4) The lowest order Amount is 3000.
5) The highest order Amount is 10000.
6) SELECT
    COUNT(*) AS Total_Orders,
    SUM(Amount) AS Total_Revenue,
    AVG(Amount) AS Average_Order_Amount,
    MIN(Amount) AS Lowest_Order,
    MAX(Amount) AS Highest_Order
FROM Orders;

🎤 Interview Question
COUNT(*) counts all the rows values including NULl, but in the other hand COUNT(column_name) does not count NULl only counts not NULl vales.
---------------------------------------------

Part A — Theory
Answer:
1. Group By groups rows that have same values.
2. We use Group By with aggregate functions to calculate all the rows with the same values.
3. 1st one adds them all together and returns one single value, while 2nd one add separately for each rows with the same values.
4. Electronics 3 and Clothing 3
5. Yes we can use where before Group By it reduce or filter by removing unwanted rows and then gives to Group By so the query run much faster.

Part B — Read the SQL
Question 1:
Alice 13000
Aman 6000
Bob 3000

Question 2:
Electronics 3
Clothing 3

Question 3:
Electronics 5333.3333
Clothing 2000

Question 4:
Alice 7000
Aman 4000
Bob 3000

Part C — Write SQL
1) SELECT Category,
SUM(AMOUNT)
FROM orders
GROUP BY Category;

2) SELECT Customer,
COUNT(*) AS Total_Number_of_order
FROM orders
GROUP BY Customer;

3) SELECT Customer,
AVG(Amount) AS Total_Average_of_order
FROM orders
GROUP BY Customer;

4) SELECT Category,
MAX(Amount) AS Highest_order
FROM orders
GROUP BY Category;

5) SELECT Customer,
SUM(AMOUNT)
FROM orders
WHERE Amount > 2000
GROUP BY Customer;

💼 Real Business Challenge
1) I use SUM aggregate function.
2) Category column I will GROUP BY.
3) SELECT Category,
SUM(Amount)
FROM orders
GROUP BY Category;
4) Electronics category generates most revenue.
5) Company use this information to give a discount or maintain their stocks.
(The company can prioritize inventory for Electronics, invest more marketing budget in high-performing products, and analyze why the category performs well. It could also use discounts carefully to increase sales in the lower-performing Clothing category.)

🎤 Interview Question
GROUP BY arranges the rows to calculate the values with the same data and Group them together. while ORDER BY keeps the the rows and rearranges them from the highest to lowest or vise versa.
(GROUP BY combines rows with the same value into groups so aggregate calculations can be performed for each group. ORDER BY sorts the final result in ascending or descending order without creating groups.)
---------------------------------------------
Due to my bad health I was not coming.
Part A — Theory
Answer:
1. An alias is a temporary name to a column or result for a meaningful name.
2. Because its took good and it is very friendly for a business report.
3.No it's does'nt change the column name in the database permanently.
4. It's allows us to add conditions into our queries like IF-ELSE.
5. CASE WHEN is the SQL equivalent of Python.
6. Because SQL stops at the first WHEN condition that is true.

Part B — Read the SQL

Question 1:
Total_Orders|Total_Revenue|Average_Order
6           | 22000       |  3666.66

Question 2:
 Customer | Amount | Order_Level
 Alice    |   5000 |High
 Aman     |   2000 |Medium
Alice     |   7000 |High
 Bob      |   3000 |Medium
 Aman     |   4000 |Medium
 Alice    |    1000|Low

Question 3:
 Customer | Order_Type
 Alice    |Big Order
 Aman     |Small Order
Alice     |Big Order
 Bob      |Small Order
 Aman     |Small Order
 Alice    |Small Order

 Question 4:
 It receive Medium because Order of WHEN condition matters in SQL, SQL stops at the first WHEN condition that is true.

 Part C — Write SQL
 1) SELECT
Customer AS Customer_Name,
Amount AS Order_Amount
FROM Orders;

2) SELECT
COUNT(*) AS Total_number_of_Orders,
SUM(Amount) AS Total_revenue,
AVG(Amount) AS Average_Order_Amount
FROM Orders;

3) SELECT
    Amount,
    CASE
        WHEN Amount >= 5000 THEN 'High'
        WHEN Amount >= 2000 THEN 'Medium'
        ELSE 'Low'
    END AS Order_Level
FROM Orders;

4) SELECT
    Customer,
    Amount,
    CASE
        WHEN Amount >= 5000 THEN 'Premium'
        WHEN Amount >= 3000 THEN 'Regular'
        ELSE 'Basic'
    END AS Customer_Type
FROM Orders;

5) SELECT
    Customer,
    Amount,
    CASE
        WHEN Amount >= 5000 THEN 'High'
        WHEN Amount >= 2000 THEN 'Medium'
        ELSE 'Low'
    END AS Order_Level
FROM orders
ORDER BY Amount DESC;

💼 Real-World Business Challenge
1. I use CASE WHEN feature to classifies the values.

2. SELECT
    Customer,
    Amount,
    CASE
        WHEN Amount >= 5000 THEN 'High'
        WHEN Amount >= 2000 THEN 'Medium'
        ELSE 'Low'
    END AS Order_Level
FROM Orders;

3. AS Order_Level

4. Alice has high value order.

5. They use this information to give that Customer some loyalty points, give then notification for upcoming new products. or any other discounts.
(The company could instead use:

Loyalty rewards
Early access to new products
Personalized recommendations
VIP programs
Retention campaigns)

🎤 Interview Question
CASE WHEN is like a IF-ELSE in SQL and we use when we need to compare the values and give result.
(CASE WHEN is similar to if/elif/else in programming. It allows us to apply conditions and create new categories or labels from existing data. For example, we can classify orders as High, Medium, or Low value based on their amount.)
---------------------------------------------

Part A — Theory

Answer:

1.COALESCE() handles missing values.
2.TRIM() removes extra space from the data.
3.UPPER() convert everything to uppercase and LOWER() convert everything to lowercase.
4.REPLACE() it replaces one value with another.
5.TRIM() removes extra space while REPLACE() removes unwanted text.
6.Why clean data?
(Data cleaning is important because dirty data can produce incorrect analysis and misleading business decisions.)

Part B — Read the SQL
Question 1:
Alice
Aman
Bob
David
Priya
(It removes all unwanted extra spaces.)

Question 2:
DELHI
MUMBAI

Question 3:
2 Customers show as a Not Provided.

Question 4:
9876543210 (It removes unwanted text.)

Question 5:
first it removes all unwanted extra space and then second it converts the result into uppercase.

Part C — Write SQL
1)SELECT
TRIM(Customer)
FROM Customers;

2)SELECT
UPPER(City)
FROM Customers;

3)SELECT
    REPLACE(Phone, '-', '')
FROM Customers;

4)SELECT
    COALESCE(Phone, 'Not Provided')
FROM Customers;

5)SELECT
    TRIM(Customer) AS Customer_Name,
    UPPER(TRIM(City)) AS Clean_City,
    COALESCE(REPLACE(Phone, '-', ''),'Not Provided') AS Clean_Phone
FROM Customers;

💼 Real Business Challenge
1. I use TRIM to remove all extra spaces.
2. I use TRIM.
3. I use COALESCE for missing phone number.
4.I use REPLACE to remove - from phone number.
5. SELECT
    TRIM(Customer) AS Customer_Name,
    UPPER(TRIM(City)) AS Clean_City,
    COALESCE(REPLACE(Phone, '-', ''),'Not Provided') AS Clean_Phone
FROM Customers;
6. It's easy to understand

🎤 Interview Question
COALESCE() returns an alternative value when a column contains NULL. For example, if a customer hasn't provided a phone number, I can use COALESCE(Phone, 'Not Provided') so the report doesn't display NULL and clearly shows that the phone number is unavailable.
---------------------------------------------

Part A — Theory
Answer:
1.To combine two tables in SQL then we can use JOIN.
2.A Primary Key is a uniquely identifies each row in a table.
3.A Foreign Key is a column that refers to a Key in another table.
4.It tell the SQL to connect the tables when their value matched.
5.An INNER JOIN returns rows where there is a matching vales in both tables.
6.Because Priya don't have a match on the other side.
7.Why do we use c.CustomerName instead of just CustomerName?
We use c.CustomerName instead of just CustomerName to tell database exactly which table the column belongs to and avoid confusion.

Part B — Read the SQL
Question 1:
6 rows will be return.

Question 2:
Alice
Aman
David
Bob
(These customers will be appear)

Question 3:
For Alice Laptop and keyboard with amount of 55000 and 1200 will be appear.

Question 4:
No Priya will be not, because its not matched with the other table.

Question 5:
Alice
Bob
Aman
(These three customers will be appear)

Part C — Write SQL
1)SELECT
    c.CustomerName,
    o.Product,
    o.Amount
FROM Customers AS c
INNER JOIN Orders AS o
    ON c.CustomerID = o.CustomerID;

2)SELECT
c.CustomerName,
c.City,
o.Product
FROM Customers AS c
INNER JOIN Orders AS o
ON c.CustomerID = o.CustomerID;

3)SELECT
c.CustomerName,
o.Product,
o.Amount
FROM Customers AS c
INNER JOIN Orders AS o
ON c.CustomerID = o.CustomerID
WHERE o.Amount > 10000;

4)SELECT
c.CustomerName,
SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerName;

5)SELECT
c.CustomerName,
SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerName
HAVING SUM(o.Amount) > 10000;

💼 Real-World Business Challenge
1) Customer Name from Customers table and Amount from Orders table.
2) CustomerID column connects them.
3) I need SUM() aggregate function.
4) GROUP BY
5) HAVING
6) SELECT
c.CustomerName,
SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerName
HAVING SUM(o.Amount) > 10000;
7) Alice, Aman, and Bob qualify.

🎤 Interview Question
INNER JOIN combines two or more tables with the matching column values.
(INNER JOIN combines rows from two or more tables where the specified join condition has matching values in both tables.
For example, I can join a Customers table and Orders table using CustomerID to find which customers placed orders.)
---------------------------------------------
Part A — Theory
Answer:
1.LEFT JOIN used to combine data from separate database tables based on to keep a complete Primary list regardless of matches.

2.which data is not matched with another table.

3.It gives a NULL value.

4.INNER JOIN combines only matched data from separate database tables while LEFT JOIN combines and keep all the data from Primary list regardless of matches.

5.Because LEFT JOIN keeps all the data regardless of matches while INNER JOIN shows only matched data.

6.Because of how a LEFT JOIN behaves when it cannot find a match.

Part B — Read the SQL
Question 1:
7 rows returned.

Question 2:
Priya has NULL for product.

Question 3:
Priya return.

Question 4:
Query A uses INNER JOIN so it's show only matched data where Query B uses LEFT JOIN so it will be shows all data from the left list regardless of matches.

Question 5:
Alice
Bob
Aman
these customers will be appears.

Part C — Write SQL
1)SELECT
    c.CustomerName,
    o.Product,
    o.Amount
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID;

2)SELECT
    c.CustomerName,
    o.Product
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID;

3)SELECT
    c.CustomerName
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID
    WHERE o.CustomerID IS NULL;

4)SELECT
    c.CustomerName,
    o.Product,
    o.Amount
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID
    WHERE o.Amount > 10000;

 5)SELECT
    c.CustomerName,
    SUM(o.Amount) AS Total_Spent
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID
    GROUP BY c.CustomerName;
(If customer has no orders then it gives a NULL value.)

💼 Real-World Business Challenge
1)Customer Name table should be the LEFT table.

2)Both tables (Customer and Orders)

3)LEFT JOIN

4)CustomerID

5)It gives a NULL vales if customer not order anythings.

6)SELECT
    c.CustomerName
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID
WHERE o.CustomerID IS NULL;

7)Priya

🎤 Interview Question

INNER JOIN It only returns rows where there is a perfect match in both tables while LEFT JOIN It returns all rows from the left table, plus any matching data from the right table. If there is no match, it fills the missing side with NULL.
---------------------------------------------

Part A — Theory
Answer:

1.RIGHT JOIN gives all data from right.
2.Right table's rows are preserved in a RIGHT JOIN,
3.In FULL OUTER JOIN it return's everything from both tables.
4.LEFT JOIN shows everything from left while RIGHT JOIN shows everything from right.
5.INNER JOIN shows only matched data from both tables while FULL OUTER JOIN shows everything from both tables.
6.A Data Analyst use FULL OUTER JOIN for data-quality checking because it's help to identify unmatched records or detect data inconsistencies from both the table.
7.LEFT JOIN
8.RIGHT JOIN

Part B — Read the SQL
Question 1:
7 rows will be return.

Question 2:
Yes because we use RIGHT JOIN so all the data from right will be there.

Question 3:
No Priya not appear because in Right JOIN only right side all data showed.

Question 4:
FULL OUTER JOIN allows us to see everything from both the tables while INNER JOIN only shows if the data is matches from the table.

Question 5:
I use LEFT JOIN to show everything from both the tables.

Part C — Write SQL
1)SELECT
    c.CustomerName,
    o.OrderID,
    o.Product
FROM Customers c
RIGHT JOIN Orders o
    ON c.CustomerID = o.CustomerID;

2)SELECT
    c.CustomerName,
    o.OrderID,
    o.Product
FROM Customers c
FULL OUTER JOIN Orders o
    ON c.CustomerID = o.CustomerID;

3)SELECT
    c.CustomerName
FROM Customers c
LEFT JOIN Orders o
    ON c.CustomerID = o.CustomerID
    WHERE o.CustomerID IS NULL;

4)SELECT
    o.OrderID,
    o.Product
FROM Customers c
RIGHT JOIN Orders o
    ON c.CustomerID = o.CustomerID
WHERE c.CustomerID IS NULL;

5)SELECT
    c.CustomerName,
    o.OrderID,
    o.Product
FROM Customers c
FULL OUTER JOIN Orders o
    ON c.CustomerID = o.CustomerID
WHERE c.CustomerID IS NULL
   OR o.CustomerID IS NULL;

💼 Real-World Business Challenge
1)LEFT JOIN
2)RIGHT JOIN
3)FULL OUTER JOIN
4)SELECT
    c.CustomerName,
    o.Product
FROM Customers c
FULL OUTER JOIN Orders o
    ON c.CustomerID = o.CustomerID;

5)Priya
6)Tablet

🎤 Interview Question
INNER → BOTH matched values
LEFT  → ALL LEFT
RIGHT → ALL RIGHT
FULL  → EVERYTHING
-------------------------------------------
#Multi-Table JOIN

Part A — Theory
Answer:

1.We can JOIN more then two tables with common matches between them it's called multi-table JOIN.
2.We need ON condition for each JOIN to tell the database exactly how to link between them.
3.In our e-commerce example, Orders table connects Customers and Products.
4.CustomerID column connects Customers to Orders?
5.ProductID column connects Orders to Products?
6.SELF JOIN means joining a table to itself.
7.We use aliases in a SELF JOIN because it treat one table as two.
8.In the employee example, e represent employee and m represent manager.

Part B — Read the SQL
Question 1:
3 tables are being joined.

Question 2:
Orders table connect both customers and Products table together.

Question 3:
CustomerName    ProductName     Amount
Alice           Laptop          55000
Aman            Mouse           700
Bob             Phone           30000
Alice           keyboard        1200

Question 4:
Because we treat one table as a two tables that's why we use two aliases for the same table.

Question 5:
No one is Rahul's manager.

Part C — Write SQL
1)SELECT
    c.CustomerName,
    p.ProductName,
    o.Amount
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID;

2)SELECT
    c.CustomerName,
    p.ProductName,
    p.Category,
    o.Amount
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID;

3)SELECT
    c.CustomerName,
    p.ProductName,
    o.Amount
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID
    WHERE o.Amount > 10000;

4)SELECT
    c.CustomerName,
    SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID
    GROUP BY c.CustomerName;

5)SELECT
    e.EmployeeName AS Employee,
    m.EmployeeName AS Manager
FROM Employees AS e
LEFT JOIN Employees AS m
    ON e.ManagerID = m.EmployeeID;

💼 Real-World Business Challenge
1)Customers
Orders
Products

2)CustomerID

3)ProductID

4)FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID
(INNER JOIN Type)

5)SELECT
    c.CustomerName,
    p.ProductName,
    o.Amount
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID;

6)SELECT
    c.CustomerName,
    p.ProductName,
    o.Amount
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID
    WHERE o.Amount > 10000;

7)SELECT
    c.CustomerName,
    SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
    ON c.CustomerID = o.CustomerID
INNER JOIN Products p
    ON o.ProductID = p.ProductID
    GROUP BY c.CustomerName;

🎤 Interview Question
A SELF JOIN is a query technique used to find hidden relationships between rows within the exact same table.
Real-World Example: An Airline Flight Network.
(A SELF JOIN is when we join a table to itself using different aliases. It's useful when rows in the same table have relationships with other rows, such as employees and their managers.)
---------------------------------------------
--Subquery--

Part A — Theory
Answer:

1.One SQL query inside an another SQL query is called subquery.

2.Usually appear when the result of one query is needed to answer another query.

3.The difference between an inner query and an outer query is their positioning and role in execution.

4.We use a subquery to automated and secure a data while manually calculating breaks the data when we make any changes.

5.The IN operator acts as a dynamic filter that allows the outer query to match a column's value against a list of multiple values returned by the inner query.

6.It basically asked does at least one matching record exist.

7.Yes

Part B — Read the SQL

Question 1:
It returns two value with their ProductName and Price.
ProductName     Price
Laptop	        55000
Phone	        30000

Question 2:
20380

Question 3:
Alice
Aman
Bob
David

Question 4:
Priya

Question 5:
It will gives Alice and Bob names because this two orders total of 20000 amount.

Part C — Write SQL
1)SELECT 
    ProductName,
    Price
FROM Products
WHERE Price >(
    SELECT AVG(Price)
    FROM Products
);

2)SELECT 
    CustomerName
FROM Customers
WHERE CustomerID IN(
    SELECT CustomerID
    FROM Orders
);

3)SELECT 
    CustomerName
FROM Customers
WHERE CustomerID NOT IN(
    SELECT CustomerID
    FROM Orders
);

4)SELECT CustomerName
FROM Customers
WHERE CustomerID IN (
    SELECT CustomerID
    FROM Orders
    GROUP BY CustomerID
    HAVING SUM(Amount) > 20000
);

5)SELECT 
    ProductName,
    Price
FROM Products
WHERE Price >(
    SELECT AVG(Price)
    FROM Products
);
Laptop And Phone these two products are qualify.

💼 Real-World Business Challenge
SELECT
    c.CustomerName,
    SUM(o.Amount) AS Total_Spent
FROM Customers c
INNER JOIN Orders o
ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerID, c.CustomerName
HAVING SUM(o.Amount) > (
    SELECT AVG(Total_Spent)
    FROM(
        SELECT
            CustomerID,
            SUM(Amount) AS Total_Spent
            FROM Orders
            GROUP BY CustomerID
    ) AS Customer_Totals
);

🎤 Interview Question
A subquery is a query nested inside another SQL query. For example, in an e-commerce platform, I could use a subquery to find products whose sales are below the average product sales and identify potentially unproductive inventory.
---------------------------------------------

Part A — Theory
Answer:

1.EXISTS check whether at least one matching exists.

2.NOT EXISTS check is their any no matching exists.

3.IN gives result as a value based and EXISTS gives result as a existence based.

4.We commonly use SELECT 1 inside EXISTS because we need at least one matched result and SELECT 1 is easy to understand and easy to read.

5.When a inner query refers to the outer query in a subquery then its called a correlated subquery.

6.c.CustomerID is the column comes from the outer query.

Part B — Read the SQL

Question 1:
Alice
Aman
Bob
David

Question 2:
Priya

Question 3:
because Priya doesn't order anythings thats way when we use EXISTS in Question 1 it doesn't appear.

Question 4:
Yes this is a correlated subquery because here the inner query refers to the outer query.

Part C — Write SQL
1)SELECT CustomerName
FROM Customers c
WHERE EXISTS(
    SELECT 1
    FROM Orders o
    WHERE o.CustomerID = c.CustomerID
);

2)SELECT CustomerName
FROM Customers c
WHERE NOT EXISTS(
    SELECT 1
    FROM Orders o
    WHERE o.CustomerID = c.CustomerID
);

3)SELECT CustomerName
FROM Customers c
WHERE EXISTS(
    SELECT 1
    FROM Orders o
    WHERE o.CustomerID = c.CustomerID
    AND o.Amount > 10000
);

4)SELECT CustomerName
FROM Customers c
WHERE NOT EXISTS(
    SELECT 1
    FROM Orders o
    WHERE o.CustomerID = c.CustomerID
    AND o.Amount > 10000
);

💼 Mini Business Challenge
1) I use NOT EXISTS SQL concept.
2) Because it gives the name of customer who have never purchased anythings yet.
3)SELECT CustomerName
FROM Customers c
WHERE NOT EXISTS(
    SELECT 1
    FROM Orders o
    WHERE o.CustomerID = c.CustomerID
);
4)Priya receive our sample data from the campaign.

🎤 Interview Question
EXISTS refers to the existence of the data when IN refers to the value of the data.
---------------------------------------------

Part A — Theory
Answer:

1.CTE stands for Common Table Expression.

2.No, It create a temporary table.

3.Because if we need to use one result for multiple times or performed additional calculations then CTEs makes easy to do it.

4.A window function performs a calculation across related rows without collapsing those rows into one row. This is the major difference from GROUP BY.

5.OVER() allow the result to don't collapse the rows.

6.It allow to Create groups for the calculation, but don't collapse the rows.

7.ROW_NUMBER() gives a unique number to the every row result and if the two orders have the same result then RANK() gives then same rank but the next value would be 2 rank gaps.

8.Both gives equal values the same rank but RANK() gives the next value 2 number gaps while DENSE_RANK() doesn't gives the 2 number gaps.

Part B — Read the SQL

Question 1:
CustomerID      Total_Spent
1	            56200
2	            15700
3	            30000
5	            700

Question 2:
Yes every row have the same vales.

Question 3:
56200
56200
15700
15700
30000
700

Question 4:
Amount  RANK
55000	1
30000	2
15000	3
1200	4
700	    5
700	    5

Part C — Write SQL

1) WITH TotalSpending AS(
    SELECT 
        CustomerID,
        SUM(Amount) AS Total_Spent
    FROM Orders
    GROUP BY CustomerID
)
SELECT *
FROM TotalSpending;

2) WITH TotalSpending AS(
    SELECT 
        CustomerID,
        SUM(Amount) AS Total_Spent
    FROM Orders
    GROUP BY CustomerID
)
SELECT *
FROM TotalSpending
WHERE Total_Spent > 20000;

3) SELECT
    OrderID,
    CustomerID,
    Amount,
    SUM(Amount) OVER(PARTITION BY CustomerID) AS Customer_Total
FROM Orders;

4) SELECT
    OrderID,
    Amount,
    ROW_NUMBER() OVER(
        ORDER BY Amount DESC) AS Row_Number
FROM Orders;

5) SELECT
    OrderID,
    Amount,
    RANK() OVER(
        ORDER BY Amount DESC) AS Rank
FROM Orders;

💼 Mini Business Challenge

1) I use window function because they want to see every order.

2) PARTITION BY

3) Yes

4) SELECT
    OrderID,
    Customer,
    Amount,
    SUM(Amount) OVER(PARTITION BY CustomerID) AS Customer_Total
FROM Orders;

5) Because GROUP BY collapse all the rows with same customer.

🎤 Interview Question
GROUP BY collapse rows while PARTITION BY doesn't.
---------------------------------------------

Part A — INSERT

Q1. INSERT INTO Customers(CustomerID, CustomerName, City)
VALUES (6, 'Rahul', 'Bangalore');

Q2. INSERT INTO Customers(CustomerID, CustomerName, City)
VALUES 
        (7, 'Neha', 'Hyderabad'),
        (8, 'Arjun', 'Chennai');

Part B — UPDATE

Q3. UPDATE Customers
SET City = 'Delhi'
WHERE CustomerID = 6;

Q4. UPDATE Customers
SET 
    City = 'Mumbai',
    CustomerName = 'Priya Sharma'
WHERE CustomerID = 4;

Part C — DELETE

Q5. DELETE FROM Customers
WHERE CustomerID = 8;

Q6. In the 1st statement it allow to DELETE row having CustomerID 8 and the 2nd statement it DELETE all the rows from the Customer Table.

Part D — DDL

Q7. ALTER TABLE Customers
ADD Email VARCHAR(50);

Q8. DELETE only DELETE rows data but DROP TABLE DELETE all the table data.

Part E — Transactions ⭐

Q9.
1-3) START TRANSACTION;
UPDATE Customers
SET City = 'Mumbai'
WHERE CustomerID = 6;
ROLLBACK;

Q10. COMMIT permanently saves the transaction.

🧠 Interview Challenge
DDL manages database structure, DML manages the actual data inside that structure, and TCL manages how those changes are saved or undone.
---------------------------------------------

🎯 Your First Assignment
Task 1:
CREATE DATABASE ECommerceAnalytics;
USE ECommerceAnalytics;

Task 2-4:
CREATE TABLE Customers(
    CustomerID INT PRIMARY KEY,
    CustomerName VARCHAR(50),
    City VARCHAR(50),
    Email VARCHAR(50)
);

CREATE TABLE Products(
    ProductID INT PRIMARY KEY,
    ProductName VARCHAR(50),
    Category VARCHAR(50),
    Price DECIMAL(10,2)
);

CREATE TABLE Orders(
    OrderID INT PRIMARY KEY,
    CustomerID INT,
    ProductID INT,
    OrderDate DATE,
    Quantity INT,
    Amount DECIMAL(10,2),
    Status VARCHAR(50),
    FOREIGN KEY (CustomerID) REFERENCES Customers(CustomerID),
    FOREIGN KEY (ProductID) REFERENCES Products(ProductID)
);
